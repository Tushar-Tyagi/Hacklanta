# Hacklanta — Core Dependencies
# ==============================
# Python 3.10+ recommended (3.14 has limited TensorFlow support)

# Ultralytics (YOLOv8) - Object Detection
ultralytics>=8.0.0,<9.0.0
torch>=2.0.0
torchvision>=0.15.0

# MediaPipe - Face Detection, Pose Estimation, Hand Tracking
mediapipe>=0.10.0

# Librosa - Audio Analysis
librosa>=0.10.0
soundfile>=0.12.0

# OpenCV - Image/Video Processing
opencv-python>=4.8.0

# Numerical Computing
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.11.0

# Image Processing
Pillow>=10.0.0

# Video Editing
moviepy>=1.0.3

# HTTP / API
requests>=2.28.0

# Audio/Video Download from URLs
yt-dlp>=2026.3.17
python-dotenv

# Progress bars
tqdm>=4.65.0

# YAML parsing (ultralytics dep, but useful standalone)
pyyaml>=5.3.1

# Optional: Web frameworks (uncomment if needed)
# flask>=2.3.0
# fastapi>=0.100.0
# uvicorn>=0.23.0

# Optional: Visualization
# tensorboard>=2.13.0

# ==============================
# Optional: Face Recognition
# ==============================
# DeepFace requires TensorFlow, which may not be available on all platforms.
# Install separately:
#
#   pip install deepface tensorflow        # Linux / Intel Mac
#   pip install deepface tensorflow-macos  # Apple Silicon (Python ≤3.12)
#
# deepface>=0.0.83

# ==============================
# Platform-Specific Notes
# ==============================
#
# macOS (Apple Silicon - M1/M2/M3/M4):
#   - TensorFlow via: pip install tensorflow-macos tensorflow-metal
#   - Requires Python ≤3.12 for TensorFlow support
#   - Some packages may require Rosetta2: softwareupdate --install-rosetta
#
# Linux (Ubuntu/Debian):
#   sudo apt-get install -y libgl1-mesa-glx libglib2.0-0 libsm6 libxext6 libxrender-dev libgomp1
#
# GPU Acceleration (NVIDIA CUDA):
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
#
# Headless Servers:
#   Replace opencv-python with opencv-python-headless